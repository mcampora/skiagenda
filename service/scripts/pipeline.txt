TBD
push on master
    module client
        build and package the module
        run unit tests for the api
merge on prod
    create if need be the artefactory bucket, deploy the api gateway
    module holidays
        build and package the module
    module resa
        build and package the module
    deploy the lambda(s)
    module client
        build and package the module
        run brush tests for the api

