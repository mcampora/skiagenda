{
  "dependencies": {
    "node-ical": "^0.15.3"
  },
  "bundleDependencies": true,
  "scripts": {
    "clean": "rm -f package-lock.json && rm -f ../../build/holidays.zip && rm -rf node_modules",
    "test": "jasmine",
    "zip": "mkdir -p ../../build && zip -r ../../build/holidays.zip ."
  },
  "name": "holidays",
  "version": "0.0.1",
  "author": "mcampora",
  "license": "Apache-2.0",
  "private": true
}
