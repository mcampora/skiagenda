{
  "name": "js",
  "main": "index.js",
  "scripts": {
    "clean": "rm -f package-lock.json && rm -rf node_modules",
    "test": "echo \"Error: no test specified\" && exit 1",
    "zip": "echo \"Error: no zip procedure specified\" && exit 1"
  },
  "version": "0.0.1",
  "author": "mcampora",
  "license": "Apache-2.0",
  "dependencies": {
    "client": "file:../../../service/packages/client"
  }
}
