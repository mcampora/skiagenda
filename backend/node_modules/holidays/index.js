module.exports = class Holidays {
    constructor(ddb) {
        this.ddb = ddb
    }

    // import current and next year schedule
    // wipe previous content
    // fetch data from an external source
    // structure and record data
    import() {
    }

}
