module.exports = class Users {
    constructor(ddb) {
        this.ddb = ddb
    }

    // offer the possibility to record and update user preferences
    // for instance color and avatar
    save(params) {
    }

}
